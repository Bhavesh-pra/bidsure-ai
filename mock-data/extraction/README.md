# Cycle 10 synthetic extraction fixtures

These expected outputs use synthetic identifiers only. They are safe fixtures
for comparing the deterministic extractor with labeled GST, PAN, and Udyam OCR
documents. Edge cases to include in test runs are scanned, multi-page, rotated,
missing-field, conflicting-field, and low-quality OCR documents.
