import React from 'react';
import type { Requirement } from '../../types';
import { Card } from '../ui/Card';
import { RequirementBadge } from './RequirementBadge';

export const RequirementDetailCard: React.FC<{ requirement: Requirement }> = ({ requirement }) => <Card title={requirement.title} subtitle="Extracted tender requirement"><dl className="grid gap-4 text-sm sm:grid-cols-2"><div><dt className="text-xs uppercase text-slate-500">Category</dt><dd className="mt-1">{requirement.category}</dd></div><div><dt className="text-xs uppercase text-slate-500">Mandatory</dt><dd className="mt-1"><RequirementBadge requirement={requirement} /></dd></div><div><dt className="text-xs uppercase text-slate-500">Expected</dt><dd className="mt-1">{requirement.expected_value ?? 'Not specified'} {requirement.unit || ''} {requirement.operator || ''}</dd></div><div><dt className="text-xs uppercase text-slate-500">Source</dt><dd className="mt-1">Tender document — {requirement.source_page ? `Page ${requirement.source_page}` : 'page unavailable'}</dd></div><div><dt className="text-xs uppercase text-slate-500">Extraction Confidence</dt><dd className="mt-1">{requirement.confidence == null ? '—' : `${Math.round(requirement.confidence * 100)}%`}</dd></div><div className="sm:col-span-2"><dt className="text-xs uppercase text-slate-500">Description</dt><dd className="mt-1 whitespace-pre-wrap text-slate-700">{requirement.description || '—'}</dd></div></dl></Card>;
